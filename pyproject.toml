[project]
name = "pdf-to-markdown"
version = "0.1.0"
description = "Convertir PDF a Markdown usando MarkItDown de Microsoft"
requires-python = ">=3.10"
dependencies = [
    "markitdown[all]>=0.0.1",
    "pillow>=9.0.0",
]
